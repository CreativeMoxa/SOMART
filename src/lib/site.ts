// Single source of truth for the public production domain, used by metadata,
// canonical URLs, the sitemap, robots.txt and JSON-LD structured data.
export const SITE_URL = "https://shopsomart.com";

// The brand as it appears on the storefront (not to be confused with the
// domain name). Used in structured data / Open Graph.
export const SITE_NAME = "SOMART";
